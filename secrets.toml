# Local development secrets file for Streamlit.
# Replace the placeholder below with your real Groq API key.
#
# IMPORTANT: Do NOT commit this file to GitHub with a real key in it.
# Add ".streamlit/secrets.toml" to your .gitignore before pushing.
#
# On Streamlit Community Cloud, don't upload this file at all — instead
# paste the same GROQ_API_KEY line into your app's Settings -> Secrets.

GROQ_API_KEY = "your-groq-api-key-here"
